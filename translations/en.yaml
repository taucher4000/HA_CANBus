configuration:
  MQTTBroker:
    name: "MQTT Broker"
    description: "Hostname or service name of your MQTT broker (e.g., core-mosquitto)."
  MQTTUser:
    name: "MQTT Username"
    description: "Optional MQTT username if your broker requires authentication."
  MQTTPassword:
    name: "MQTT Password"
    description: "Optional MQTT password if your broker requires authentication."